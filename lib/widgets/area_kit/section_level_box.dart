import 'package:flutter/cupertino.dart';

class SectionLevelBox extends StatelessWidget {
  const SectionLevelBox({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
